# Normalize line endings to LF for text files across platforms.
* text=auto eol=lf

# Treat common binary assets as binary (uploads are gitignored anyway).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
